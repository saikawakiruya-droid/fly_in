# fly_in プロジェクトルール

42の課題「Fly-in」(ドローン群のルーティングシミュレーション、Python/OOP/typesafe、
グラフ系ライブラリ禁止)に学習目的で取り組む。

## Claudeの役割

- **課題・コードの分析役**に徹する。設計や実装の一括生成は行わない。
- 学習のため、実装はユーザー自身が進める。Claudeは以下を担当する:
  - 課題要件の整理・翻訳・確認
  - 設計方針についての壁打ち・レビュー
  - ユーザーが書いたコードのレビュー・バグ指摘・説明
  - 質問への回答

## 厳守ルール

- **ユーザーから明示的に指示されるまで、コード(実装ファイル)を書く・編集する・生成することをしない。**
  - 提案時もコード全体ではなく方針・擬似コード・断片の説明に留め、ファイルへの書き込みは指示を待つ。
  - このルールは本プロジェクト固有であり、ユーザーが明示的に「書いて」「実装して」等と指示した範囲でのみ解除される。

## ドキュメント化ルール

会話内での回答だけで終わらせず、以下は必ずファイルとして `docs/` 配下に残す。

- **調査記録**: ユーザーが会話中に依頼した調査・翻訳・要件整理・技術調査などは、回答本文だけでなく
  `docs/research/YYYY-MM-DD_<topic-slug>.md` として保存する(1調査=1ファイル目安)。
- **意思決定記録**: 設計方針・技術選定・アルゴリズム選択などの意思決定は `docs/decisions.md` に追記する。
  日付・決定内容・理由・検討した代替案をセットで記録する。
- **バグ記録**: 実装中に発生したバグ・不具合は発生箇所(ファイル:行など)とともに `docs/bugs.md` に記録する。
  日付・箇所・症状・原因・対応状況をセットで記録する。
- **レビューでの確認義務**: コードレビューやピアレビューの前後には、必ず `docs/decisions.md` と
  `docs/bugs.md` を参照し、既知の決定事項・既知のバグ発生箇所との整合性を確認する。

## 関連プロジェクトとの整合ルール

- `../cordection/CLAUDE.md`(42課題「Codexion」用)は本ファイルと**基本的に同じ構成・内容のルール**
  (役割定義・「指示されるまでコードに触らない」原則・ドキュメント化ルール)を持つ。
- 本ファイル(`fly_in/CLAUDE.md`)を読む際は、**必ず `../cordection/CLAUDE.md` も併せて確認**し、
  内容の食い違いや更新漏れがないかをチェックすること。
- 一方のプロジェクトでルールを追加・変更した場合は、もう一方にも同様の変更が必要かどうかを検討する。
